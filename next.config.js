/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
      domains: ['images-assets.nasa.gov'], // Adiciona o domínio externo da NASA para carregar imagens
    },
  };
  
  module.exports = nextConfig;
  